@model IEnumerable<GiftStoreMVC.Models.GiftstoreCategory>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_MainDashboardLayout.cshtml";
}

<div class="page-wrapper">
    <div class="page-container">
        <div class="main-content">
            <div class="section__content section__content--p30">
                <div class="main">
                    <ul class="cards">
                        @foreach (var item in Model)
                        {
                            <li class="cards_item">
                                <div class="card">
                                    <div class="card_image">
                                        <a asp-action="CategoryGifts" asp-route-Categoryid="@item.Categoryid">
                                            <img src="@Url.Content("~/CategoriesImages/"+item.Imagepath)" alt="mixed vegetable salad in a mason jar." />
                                        </a>
                                        <span class="card_price"><span>$</span>9</span>
                                    </div>
                                    <div class="card_content">
                                        <h2 class="card_title">@item.Categoryname</h2>
                                        <div class="card_text">
                                            <p>
                                                Admin name: <strong>Abdullah</strong>
                                            </p>
                                            <hr />
                                            <p>
                                                @item.Categorydescription
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
